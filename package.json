{
  "name": "meeting-office",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n client,server,livekit,netplay -c cyan,magenta,yellow,green \"vite\" \"npm run start:dev -w meeting-office-server\" \"node scripts/start-livekit.mjs\" \"node scripts/start-netplay.mjs\"",
    "dev:client": "vite",
    "dev:server": "npm run start:dev -w meeting-office-server",
    "dev:livekit": "node scripts/start-livekit.mjs",
    "dev:netplay": "node scripts/start-netplay.mjs",
    "test": "npm test -w meeting-office-server",
    "build": "tsc --noEmit && vite build && npm run build -w meeting-office-server",
    "preview": "vite preview"
  },
  "dependencies": {
    "livekit-client": "^2.22.0",
    "phaser": "^4.2.1"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "typescript": "^5.9.2",
    "vite": "^7.1.2"
  }
}
